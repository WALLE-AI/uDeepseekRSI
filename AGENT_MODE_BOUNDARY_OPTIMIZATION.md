# 办公 / 编码 / 研究 三模式能力边界优化方案

> 本文接续 [WORK_MODE_SOLUTION.md](./WORK_MODE_SOLUTION.md)（模式如何落地）和 [WORK_MODE_COMPARISON.md](./WORK_MODE_COMPARISON.md)（模式差异现状），解决的是下一个问题：**三个模式各自该管什么、不该管什么，以及这条边界如何被系统真正执行。**

## 0. 术语前提：模式不是智能体

本方案采用两级模型。**办公 / 编码 / 研究是三种"模式"，不是三个智能体**：

```text
模式 Mode（场景编排域 · 用户可见的一级选择 · 会话级不可变）
  └── 专家智能体 Expert Agent（模式内的职能单元 · 可以是 1 个或多个 · 系统内部调度）
```

两级的性质截然不同，后文所有设计都建立在这个区分上：

| 维度     | 模式之间                         | 模式内的专家之间                      |
| -------- | -------------------------------- | ------------------------------------- |
| 关系     | **互斥**：一个会话只属于一个模式 | **互补**：同一任务内协作完成          |
| 可见性   | 用户显式选择                     | 系统内部调度，用户可观察但不必选择    |
| 边界目的 | 避免职责重叠与选择困难           | 收敛每个单元的注意力、工具与权限      |
| 变更代价 | 需新建会话（或走移交）           | 由模式主控在同一会话内动态委派        |
| 对应范式 | handoff（控制权转移）            | manager / agent-as-tool（中心化编排） |

因此"能力边界"是两个问题而不是一个：**模式间边界**决定用户选什么，**模式内专家边界**决定一个模式如何把事做好。当前实现只有前者的雏形，后者完全不存在（见 1.3）。

## 1. 现状盘点（基于代码事实）

### 1.1 三模式当前的全部差异

| 差异项         | 实现位置                                                | 说明                                            |
| -------------- | ------------------------------------------------------- | ----------------------------------------------- |
| 助手 ID        | `DshRuntimePool.ts:4-11`                                | `dsh:office` / `dsh:coding` / `dsh:research`    |
| System persona | `personaForDshWorkMode()`，经 `AIONUI_DSH_PERSONA` 注入 | 三段硬编码英文提示词                            |
| DSH 运行时     | `DshRuntimePool` 按模式懒启动独立 DSH 进程              | 进程级隔离                                      |
| DSH_HOME       | `DshApiServer.ts:793`                                   | coding 用原 home，其余为 `dshHome/modes/<mode>` |
| 排序与图标     | `sort_order = DSH_WORK_MODES.indexOf(mode)`、前端图标   | 办公 0 / 编码 1 / 研究 2                        |
| 预热           | `prewarmMode` 默认 `'office'`                           | 首屏默认模式                                    |

### 1.2 三模式当前完全相同的部分

- 同一份 patch（`.aionui/dsh-aionui.patch.yml`）：同样的 provider、模型列表、web-search、web-fetch。
- 同一技能根：`AIONUI_SKILLS_DIRS_JSON=[skillsDir]`，三模式看到同一套技能（`DshApiServer.ts:800`）。
- 同一沙箱：`sandbox-policy.mode: workspace-write`，`workspaceRoot: process.cwd()`。
- 同一审批：`approval.policy: ask`。
- 同一 MCP 池与同一套解析规则。
- 同一助手默认配置：`defaultAssistantConfig()` 对三个模式返回完全一致的空配置（`enabledSkills: []`、`skills/mcps` 为空 fixed）。
- 同一（空的）推荐提示词：`prompts: []`、`prompts.recommended: []`。

结论：**当前三模式是"同一套能力 + 三段提示词 + 三个进程"，边界只存在于 persona 文本里，没有任何一层系统机制在执行它。**

### 1.3 模式内专家层：当前为零

按第 0 节的两级模型核对代码，**当前每个模式恰好等于一个智能体，专家层完全不存在**：

- `#assistantRecord()` / `#assistantDetailRecord()` 对三个模式都写死 `team_selectable: false`，`team_block_reason: 'Direct deepseek-harness sessions are single-agent.'`（`DshApiServer.ts:3125`、`3139`）。
- `/api/teams` 与 `/api/extensions/acp-adapters` 一并返回空集合（`DshApiServer.ts:3039`），AionUi 自身的 Team 多智能体编排对 Direct DSH 后端整体关闭。
- 每个模式一个 DSH 进程、一个 ACP session、一条 persona，没有任何子智能体注册。

但**底层能力是具备的**。DSH 原生提供子智能体机制（`ctx.subagents` 服务 + spawn / fork 两个进程内后端，共用 `@deepseek-ai/dsh-subagent-in-process-driver` 的 `startInProcessRun`），关键语义对本方案非常有利：

| 机制特性                                                                                      | 对专家层的意义                             |
| --------------------------------------------------------------------------------------------- | ------------------------------------------ |
| 创建子智能体时可安装**独立 persona、工具限制与结构化输出**                                    | 每个专家可以有自己的职责与产出契约         |
| 子智能体获得**全新扁平注册作用域，不继承父的工具限制与权限**                                  | 按专家收敛工具是可行的，但**必须显式配置** |
| 子智能体默认继承父的 provider / model / reasoning effort / 输出上限，可由 `agentOptions` 覆盖 | 可为不同专家配置不同推理档位               |
| 父的 sandbox override 与 `'never'` 审批钉选**会传播给子智能体**                               | 安全注意点，见 10 节                       |
| 有深度限制（子深度 = 父深度 + 1，支持 `maxDepth`）                                            | 可防止专家层无限递归                       |

也就是说，专家层缺的不是引擎能力，而是**编排设计与配置**。

## 2. 问题诊断

### P1 边界维度选错了：按"话题域"切分导致结构性重叠

现有 persona 直接自证了重叠：

- 办公 persona 写着 `documents, spreadsheets, presentations, PDFs, research, and data organization` —— 把 research 写进了办公职责。
- 研究 persona 要求产出 `literature review / market or competitor analysis / due diligence / policy comparison` —— 这些交付物形态正是办公模式的主场。

于是"整理一批调研材料并出一份报告"这种最高频的任务，在办公和研究两个模式下都成立，用户无从选择，模型也无从判断。

### P2 模式没有兑现它承诺的专业能力

- 办公模式没有默认启用任何 Office 处理技能（`enabledSkills: []`），"更擅长办公"仅来自提示词。
- 研究模式没有独占的检索能力：`web-search-deepseek` 对三模式一视同仁地开启。
- 编码模式没有独占的仓库 / 测试 / 版本控制能力。

per-mode 差异化的**机制已经存在**（`StoredAssistantConfig` 支持按模式配置 skills / mcps / model / permission / thought_level），但**默认值全为空**，机制处于空转状态。

### P3 权限边界为零

三模式共用 `workspace-write` + `approval: ask` + 同一个 `workspaceRoot`。这意味着：

- 研究模式（本应以只读检索为主）拥有与编码模式完全相同的写盘和执行权限；
- 办公模式可以直接改写用户仓库源码；
- 一个"整理资料"的会话和一个"重构模块"的会话，风险面完全相同。

另外 `workspaceRoot: process.cwd()` 取的是 DSH 进程的 cwd（即 `options.cwd`），不是会话 cwd，等于全局一个固定根。

### P4 边界对用户不可见

- `#workModeDescription()` 返回英文硬编码字符串，没有走 i18n（只有模式名走了 `agentMode.work.*`）。
- 前端 pill 只显示图标 + 名称，不显示职责说明。
- 三个模式的推荐提示词都是空的 —— 教用户"这个模式适合干什么"最廉价的手段没有使用。

### P5 默认模式与真实入口错配

`pickDefaultAssistantSelectionKey()` 固定优先 office，`prewarmMode` 也固定 office。但当用户打开的工作区是一个代码仓库时，最可能的意图是 coding，此时既选错了模式，也预热错了进程（白付一次冷启动）。

### P6 一次性选择，没有跨模式通路

模式是会话级不可变身份（这个设计是对的），但真实任务天然跨域：研究 → 成稿、编码 → 写发布说明、办公 → 需要开放取证。当前唯一出路是用户手动新建会话并重新交代上下文，产出物和结论全部丢失。

### P7 没有边界的度量

`work_mode` 目前只进入延迟日志（`#logLatency`）。没有任何机制回答"persona 是否真的产生了稳定的行为差异"，也就无法判断优化是否有效。

### P8 模式被实现成了单个智能体，缺少专家层

这是第 0 节两级模型暴露出的结构性缺口，也是 P2 的更深层原因。

当一个模式只有一个智能体时，这个智能体必须同时承担该模式内的全部职责——办公模式的同一个上下文里既要做数据整理、又要做文档撰写、还要做校对；编码模式的同一个上下文里既要勘察仓库、又要实现变更、还要自我审查。结果是：

- **注意力与上下文预算被稀释**，每项职责都在和其他职责争夺权重；
- **工具集无法收敛**，一个智能体必须持有该模式所有职责的工具并集；
- **读写权限无法分离**，勘察与实现共用同一套权限；
- **自我审查失效**，同一上下文写完代码再自审，天然偏袒自己的产出。

换句话说，P1 的"模式间重叠"和 P8 的"模式内混同"是同一个病的两面：**职责边界没有在任何一个层级上被建模。**

## 3. 外部实践校准

本节用行业实践反向校验第 4、5 节的边界划分。取样三类来源：头部产品对同类智能体的能力定义、多智能体角色划分的工程实践与反模式文献、智能体最小权限与沙箱的安全指引。

### 3.1 办公场景：智能体操作的是"制品结构"，不是文本

外部产品对办公智能体的定义有一个高度一致的特征：**直接操作 Office 制品的结构，而不是返回一段文本让用户自己粘贴回去。**

- Microsoft 365 Copilot 的 Word / Excel / PowerPoint Agents 从自然语言直接生成完整制品——Excel Agent 可生成带公式的多工作表工作簿，PowerPoint Agent 生成带视觉元素的幻灯片——并可在原生应用内执行多步修改。它们解决的是"空白页问题"。([Microsoft Learn](https://learn.microsoft.com/en-us/microsoft-365/copilot/wordexcelppt-agents)、[Microsoft 365 Copilot Blog](https://techcommunity.microsoft.com/blog/microsoft365copilotblog/introducing-word-excel-and-powerpoint-agents-in-microsoft-365-copilot/4470604))
- Claude for Excel 的差异化不在"会写公式"，而在**结构感知 + 单元格级引用 + 变更可追溯**：读多表工作簿、解释计算逻辑并让用户跳转到被引用的单元格、在保持公式依赖的前提下更新假设，并完整披露自己做过的修改。([Anthropic](https://www.anthropic.com/news/advancing-claude-for-financial-services)、[The Register](https://www.theregister.com/2025/10/28/anthropic_claude_excel/))
- 办公智能体的数据面是**用户有权访问的组织内材料**（M365 通过 Work IQ 从文件、邮件、会议、站点取数，并遵守用户权限），而不是开放网络。([Microsoft Learn](https://learn.microsoft.com/en-us/microsoft-365/copilot/wordexcelppt-agents))

对本方案的含义：

1. 办公模式的成功判据应写成"制品结构正确 + 可溯源到素材 + 不破坏既有格式与公式依赖"，而不是模糊的"写得好"。
2. 办公模式必须具备真正的文件结构操作能力（officecli 类技能默认启用），否则它只是一个语气不同的对话模式——这正是 P2 的要害。
3. **"数据面是用户已有素材而非开放网络"这条外部共识，直接支持本方案把开放取证划归研究模式独占。**
4. 变更透明（改了哪些文件、哪些单元格与段落）应成为办公模式的汇报硬要求。

### 3.2 编码场景：范围控制与可验证性是第一性的

- **范围不定义，智能体会替你定义。** 通行做法是先让智能体给出 5–7 步计划再动手，产出"一个 commit 量级"的小 diff，一次迭代只承载一个意图，不把 bugfix、重构与风格调整混进同一个补丁。([Claude Code Best Practices](https://code.claude.com/docs/en/best-practices)、[OpenHands](https://www.openhands.dev/blog/claude-code-best-practices-agentic-coding))
- **验证不可省略。** 核心规则是"无法验证就不要交付"：改完立刻跑测试与构建（就近验证），把失败日志喂回去；"能用"必须等于"通过测试且满足验收标准"。被中断或超时的检查绝不能被报告为通过。([Claude Code Best Practices](https://code.claude.com/docs/en/best-practices))
- **仓库约定文件要给确定性命令**——"提交前构建"对智能体没有意义，`./gradlew build` 才有。同时明确：`CLAUDE.md` 是指引，不是安全边界。([BSWEN: AGENTS.md 实践](https://docs.bswen.com/blog/2026-08-24-how-to-write-agents-md/)、[Claude Code Best Practices](https://code.claude.com/docs/en/best-practices))
- **最大的错误是宽权限配弱验证。** 访问控制要靠权限系统、凭证范围、沙箱和下游系统自身的授权来落实，无人值守场景用工具允许列表收敛。([Claude Code Best Practices](https://code.claude.com/docs/en/best-practices))

对本方案的含义：

1. 编码 persona 现有方向正确但强度不足，应显式要求"先给小步计划 → 产出可回退的小变更 → 就近验证 → 报告验证状态与剩余风险"。
2. "已完成"必须绑定证据（执行的命令与结果）。
3. **"约定文件不是安全边界"这条直接印证 P3**：写盘范围不能靠 persona 约束，必须由沙箱（L3）执行。

### 3.3 研究场景：产出是"结论 + 可核验证据"，且引用可靠性是已被量化的风险

- 深度研究的标准形态是**自主多步推理与检索**：计划 → 迭代检索 → 识别知识缺口 → 再检索 → 结构化综述并附引用，一次请求常涉及数十次检索、15–30 分钟的后台运行。典型任务域是法律分析、市场研究与科学文献综述。([ByteByteGo](https://blog.bytebytego.com/p/how-openai-gemini-and-claude-use)、[IntuitionLabs](https://intuitionlabs.ai/articles/chatgpt-deep-research-guide-ai-agents-rag))
- 输出契约包含**每条主张的粒度级引用与 grounding metadata**（来源 URL、实际使用的检索词），使用户可以逐条核验；同时支持把用户文档（PDF / CSV / doc）与公网资料一起综合。([Gemini API 文档](https://ai.google.dev/gemini-api/docs/deep-research)、[Google 开发者博客](https://blog.google/technology/developers/deep-research-agent-gemini-api/))
- **引用可靠性风险有实证量化**：一项覆盖 10 个模型与智能体、5.3 万条 URL 的研究发现，3–13% 的引用 URL 是凭空捏造的（Wayback Machine 无任何记录），5–18% 无法解析；**深度研究智能体尽管生成的引用最多，幻觉率反而最高**——检索架构比输出量更决定质量，"引用更多"不等于"每条引用更准"。([arXiv 2604.03173](https://arxiv.org/abs/2604.03173))
- 更根本的问题是**引用无法被验证**：当前做法要么信任模型自引，要么使用不校验可达性、主题相关性与事实一致性的 RAG。高链接有效率会制造虚假信任，在医疗、法律、金融等高风险领域尤其危险。([Cited but Not Verified, arXiv 2605.06635](https://arxiv.org/pdf/2605.06635))
- 失败类型需要区分：**statement hallucination**（内容偏离所引来源）与 **citation hallucination**（引用本身捏造）。([ReportBench, arXiv 2508.15804](https://arxiv.org/pdf/2508.15804))
- 端到端评测会掩盖中间过程的幻觉，应改为**过程感知评测**，审计 plan–search–summarize 完整轨迹。([arXiv 2601.22984](https://arxiv.org/abs/2601.22984))
- 缓解手段有效且成本低：为模型配备 URL 存活性检查工具后，不可解析引用下降 6–79 倍至 1% 以下。([arXiv 2604.03173](https://arxiv.org/abs/2604.03173))

对本方案的含义：

1. 研究模式增加一条**硬性交付要求：引用可达性自检**。交付前校验引用 URL 可解析，标注失效与存疑来源。这是有实证收益的低成本工程动作。
2. 研究模式需要**后台长时运行与进度可见**（典型 15–30 分钟、数十次检索）。这是原方案遗漏的能力项，需补入。
3. 边界度量（第 8 节）应包含过程级检查，而不只看终稿。
4. **研究模式产出"结论 + 证据"、成稿排版交给办公模式，与外部产品分工一致**——Gemini Deep Research 交付的是报告与 grounding metadata，不负责生成 pptx。

### 3.4 边界方法论：重叠是已被命名的反模式

- 反模式文献直接命中 P1：给多个智能体分配**模糊、冗余或重叠的职责**（例如多个"研究"角色）而不区分范围、权限与目标，会导致认知冗余、循环委派、输出冲突与任务解析不一致；根因之一是"按职位名而不是按能力划分角色"。**我们按"话题域"划分模式，正是这种错误的一个变体。**([Anti-Patterns in Multi-Agent Gen AI Solutions](https://medium.com/@armankamran/anti-patterns-in-multi-agent-gen-ai-solutions-enterprise-pitfalls-and-best-practices-ea39118f3b70)、[AI Agent Anti-Patterns Part 1](https://achan2013.medium.com/ai-agent-anti-patterns-part-1-architectural-pitfalls-that-break-enterprise-agents-before-they-32d211dded43))
- 角色边界的规范写法：为每个角色定义**唯一目标、输入/输出格式、决策范围与可用工具**，使用结构化角色定义，并通过 dry simulation 验证职责不重叠。学术表述把角色画像定义为"该智能体是谁、掌握什么能力、追求什么结果、责任边界在哪"，其目标陈述必须避免与其他岗位重叠。([Anti-Patterns in Multi-Agent Gen AI Solutions](https://medium.com/@armankamran/anti-patterns-in-multi-agent-gen-ai-solutions-enterprise-pitfalls-and-best-practices-ea39118f3b70)、[arXiv 2512.08769](https://arxiv.org/pdf/2512.08769))
- 单一职责对智能体同样成立：每增加一项职责都在争夺注意力预算、上下文预算与行为一致性。([arXiv 2512.08769](https://arxiv.org/pdf/2512.08769)、[AI Agent Anti-Patterns Part 5](https://achan2013.medium.com/agent-anti-patterns-part-5-05da1c3c1828))
- 但**不要过度拆分**：智能体更多不等于设计更好；工具目的重叠会主动干扰智能体选择高效策略，而"需要加新工具来处理边缘情况"往往说明任务范围该缩小，而不是工具该增加。([MachineLearningMastery](https://machinelearningmastery.com/building-ai-agents-here-are-some-anti-patterns-to-avoid/)、[Google Cloud 架构中心](https://docs.cloud.google.com/architecture/choose-design-pattern-agentic-ai-system))

**这两条合起来支撑本方案的核心取舍：重新划界而不是新增第四个模式。**

### 3.5 移交与路由：两个可直接借鉴的成熟范式

- **Handoff**：一个智能体把对话控制权交给更专精的智能体，接手方看到完整历史并接管后续，原智能体不再回到循环；与"管理者 / 智能体即工具"范式相对。实现上 handoff 对模型暴露为工具（如 `transfer_to_refund_agent`），可用 `input_filter` 控制接手方看到的历史，并用 `on_handoff` 钩子做可观测。官方建议是"优先使用各自精通一件事的专精智能体，而不是一个被期望样样都行的通用智能体"。([OpenAI Agents SDK: Handoffs](https://openai.github.io/openai-agents-python/handoffs/)、[Agent orchestration](https://openai.github.io/openai-agents-python/multi_agent/))
- **路由**：分类层（规则 → 向量匹配 → LLM 分类）比把全部工具塞给模型自选更可靠。工具规模上升时选择准确率显著退化（50 个工具约 94%，200 个降到 64%，417 个降到 20%）。([tianpan.co](https://tianpan.co/blog/2026/04/16/intent-classification-agent-routers)、[Taskade](https://www.taskade.com/blog/ai-agent-routing-patterns))
- 路由的失败模式应当被显式测试：非正式或含糊表述、一条消息含多个诉求（关键要求是不能静默丢弃其中一个）、处于类别边缘的输入。([Microsoft AI Agent Eval Scenario Library](https://github.com/microsoft/ai-agent-eval-scenario-library/blob/main/business-problem-scenarios/triage-and-routing.md))

对本方案的含义：第 7 节的移交机制与 handoff 范式同构，两个细节值得吸收——**接手方必须拿到足够历史**（本方案用移交摘要实现），以及**移交事件要可观测**（用于第 8 节度量）。第 6 节 L4 的上下文感知默认模式属于轻量规则路由；未来若加意图分类器做模式推荐，仍应由用户确认而非自动跳转。

### 3.6 权限：最小权限要按"离散任务"建模，且读写分离

- 最小权限意味着只授予**当前任务所需**的数据、系统与动作访问，且只在任务期间有效。难点在于智能体运行时自行决定动作，部署时看着安全的静态权限集，可能被以无人预料的方式行使。([Microsoft Security Blog, 2026-07](https://www.microsoft.com/en-us/security/blog/2026/07/16/least-privilege-for-ai-agents-identity-access-and-tool-binding/))
- 大多数事故的根因是**环境权限（ambient authority）**：进程自动继承执行环境的全部背景权限。遇到提示注入时，攻击者无需"入侵"，只需让这个"糊涂代理"使用它本就持有的权限。传统 RBAC 挡不住，因为它校验的是请求进程的身份而非请求的来源。([Cosmonic](https://cosmonic.com/blog/ai-sandbox-guide/)、[Northflank](https://northflank.com/blog/how-to-sandbox-ai-agents))
- 建议按**离散任务而非团队或组织结构**建模角色（例如"只读知识检索"、"创建工单草稿"）；当一个工作流同时包含取证与处置时，用不同角色或不同工具**分离读与写**；删除、导出、提权等高影响动作走升级审批。权限应受资源边界、数据边界与操作边界三重约束。([Microsoft Security Blog](https://www.microsoft.com/en-us/security/blog/2026/07/16/least-privilege-for-ai-agents-identity-access-and-tool-binding/))
- 实用策略形状：**默认无访问，只读是廉价的例外，任何有真实副作用的动作过人**——只读自动执行，写入 / 删除 / 推送 / 花钱暂停等待人工。([pushary](https://pushary.com/blog/what-permissions-should-an-ai-agent-have)、[Arthur](https://www.arthur.ai/column/ow-to-apply-least-privilege-to-ai-agents))
- 同时承认代价：任务级角色需要额外设计，JIT 访问带来生命周期复杂度，审批引入摩擦。"事事请示的智能体算不上自治，从不请示的迟早会做出让你后悔的事"——答案是**风险分层自治**。([Microsoft Security Blog](https://www.microsoft.com/en-us/security/blog/2026/07/16/least-privilege-for-ai-agents-identity-access-and-tool-binding/))

对本方案的含义：第 6 节 L3 的按模式沙箱设计与该指引一致，并可据此精确化——**研究模式默认只读、办公模式写入限于交付目录、编码模式写入项目根**，正是"按离散任务建模 + 读写分离"的落地形式。而"风险分层自治"支持本方案第 5 节的软边界取舍：首次越界说明一次，用户坚持则执行。

### 3.7 腾讯 WorkBuddy：专家与专家团的产品级参照

WorkBuddy 是与本项目形态最接近的参照系——同为桌面端、本地执行、面向办公场景的 AI 智能体工作台（腾讯云 CodeBuddy 团队，2026-03-09 发布），且它已经把"模式 / 专家 / 团队"这套两级模型做成了产品。它的取舍值得逐条对照。

#### 三层能力体系与职责边界

WorkBuddy 把能力拆成三个不可互相替代的层：

| 层级       | 定义                                                   | 回答的问题                    |
| ---------- | ------------------------------------------------------ | ----------------------------- |
| **Skill**  | 封装可执行脚本与工作流，定义触发条件、步骤、边界与输出 | AI **按什么流程做事**         |
| **专家**   | 能力 + 经验 = **人设 + 方法论 + 工具链**               | AI **以什么身份、用什么方法** |
| **专家团** | 多位专家 + 协作流程                                    | 多角色**如何分工协作**        |
| MCP        | 与外部服务的标准化对接通道                             | AI **能调用什么外部工具**     |

边界规则很清晰：一个 Skill 可以调用 MCP，但二者不能互相替代。官方给出的选型速查是——需要某种工具能力就加 Skill，有明确的单点问题就用单专家，任务复杂需要多角色配合才上专家团。([知乎：WorkBuddy 能力体系](https://zhuanlan.zhihu.com/p/2047676983740470493)、[七牛云：进阶教程](https://news.qiniu.com/archives/1786500985404))

#### 专家的定义与生成

- 专家是一种**角色切换机制**，让系统以特定领域专家的身份和视角执行任务。定义公式是**人设 + 方法论 + 工具链**。
- 官方预设 100+ 领域专家，按行业分类；「我的专家」支持自定义。
- **可用自然语言创建专家**，背后是一个内置的 `expert-manager` skill（"设计 subagent"的 skill）——即"用智能体造智能体"的元能力。
- 企业版支持**上传岗位说明书（JD）定制企业专属专家**，被概括为"把岗位说明书写进 AI 里"。([专家中心文档](https://www.workbuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Expert-Center)、[知乎：专家团新功能](https://zhuanlan.zhihu.com/p/2045911727477469619))

#### 专家团的组建与编排

组建是**对话式的、自动的**：用大白话说明领域 → 系统交互式问答收集需求 → 自动匹配 Skill / 插件 / MCP 工具链 → 生成可直接执行的专属团队。

编排是**「团长 + 并行子专家」**：团长专家自动拆解任务、分配给不同专家并行执行、最后合并输出。典型例子——竞品分析报告拆成市场规模分析、功能对比、定价调研三路并行；内容创作专家团由策略（定选题）、文案（起草）、设计（出视觉）、编辑（润色）、跨语言适配组成流水线。

关键产品决策：**用户不需要配置工作流，也不需要知道底层跑了几个智能体。**([知乎：专家团新功能](https://zhuanlan.zhihu.com/p/2045911727477469619)、[Pandaily](https://pandaily.com/tencent-workbuddy-enterprise-agent-suite-strategy-jun2026))

#### 三条最有参考价值的约束

1. **跨团召唤走新会话。** 在专家团会话中召唤其他专家时，系统提示需开启新对话，**新专家不会回绑到当前专家团会话**。这与本方案"模式是会话级不可变身份、跨界走移交新建会话"的设计完全同构，是一个产品级的独立验证。
2. **专家不是权限主体。** 专家本身不主动获取系统权限，仅处理用户提供的对话与上传文件；**实际的外部动作要靠挂载的 Skill / MCP**。也就是说权限跟随工具走，角色只决定行为倾向。
3. **专家团的成本是单专家的 3–5 倍。** 团长拆解、并行执行、整合交付都要额外的模型交互。这给"何时该委派"提供了量化尺度。

另外，WorkBuddy 把任务级配置规范成四个问题——**这次任务让哪个 Agent、带着哪些材料、用什么权限、在哪个目录里干活**，是很干净的产品化表述。

#### 安全与资产沉淀

- 执行侧通过沙箱、工作区隔离、文件访问权限、敏感操作确认来管控，Skill 安装前做安全扫描。
- 自定义 Skill 不应直接投产，至少要测试**正常输入、缺失输入、工具失败**三种情况，并检查是否越权写文件、发送消息或调用未授权 MCP，并保留可回退版本。
- 项目空间中调试完成的专家、Skill 与连接器**可沉淀为资产**，一次配置多人复用；企业版进一步提供组织架构、成员授权、治理审计、用量统计与成本分析。([Tencent Cloud](https://www.tencentcloud.com/techpedia/145645?lang=en)、[网易：企业版发布](https://www.163.com/dy/article/KUM46N730511DSSR.html)、[知乎：硬核实战](https://zhuanlan.zhihu.com/p/2077749253108192294))

#### 对本方案的五点修正

| 编号 | 吸收项                                                                                       | 影响章节 |
| ---- | -------------------------------------------------------------------------------------------- | -------- |
| W1   | 专家定义补 **`method`（方法论）** 字段——原 5.3 字段只有人设与工具链，缺"按什么步骤工作"      | 5.3      |
| W2   | **权限跟随工具，沙箱兜底**——`access` 不再独立配置，由 `allowedTools` 推导，沙箱作为上限校验  | 5.3、L3  |
| W3   | **委派是升级路径而非默认行为**——以 3–5 倍成本为尺度，简单任务主控直接做                      | 7.2、10  |
| W4   | 借鉴选型速查，把"何时加 Skill / 何时单专家 / 何时委派"写成主控 persona 的委派判据            | L1、7.2  |
| W5   | 专家定义应是**可枚举、可沉淀的资产**而非硬编码提示词；自然语言造专家列为后续演进，第一版不做 | L1、10   |

其中 W1 与 W2 是对 5.3 规范字段的实质修正，W3 与 W4 补上了本方案原先缺失的"委派触发条件"。

### 3.8 WorkBuddy 机制的工程解读

本节依据仓库内的 [`opensource/workbuddy/我用阿里 AgentScope 复刻了一个 WorkBuddy：专家团是怎么跑起来的.md`](./opensource/workbuddy/我用阿里%20AgentScope%20复刻了一个%20WorkBuddy：专家团是怎么跑起来的.md)（叶小钗，2026-08-12）。该文包含两类内容，引用时需要区分：**对 WorkBuddy 实际产物的观察**（专家目录结构、`plugin.json` 字段、主理人规则），以及**作者用 AgentScope 复刻时的工程实现**（加载器、缓存指纹、事件投射、异步生命周期）。后者不是 WorkBuddy 官方设计，但因为踩的是同一类坑，对本方案的参考价值反而更高。

#### 专家的五要素（修订 3.7 的三要素）

该文把专家拆成五个核心要素，比"人设 + 方法论 + 工具链"更完整：

| 要素         | 含义                                                     |
| ------------ | -------------------------------------------------------- |
| 人设         | 专家是谁、什么专业背景、说话风格                         |
| 方法论       | 该领域的标准工作流（如写作："选题→大纲→初稿→修改→定稿"） |
| 工具链       | 绑定了哪些 Skill、MCP 连接器和内置工具                   |
| **工作模板** | 常见任务的输出格式（如数据分析师的报告结构）             |
| **沟通风格** | 输出是简洁要点还是详细分析、用不用表格                   |

并给出一条本质判断：**专家要按一套相对稳定的办法做事，不能每次都靠模型临场发挥。** 这正是 `method` 字段存在的理由。

#### 专家是一个自包含目录包

WorkBuddy 选择专家后会把该专家的文件下载到本地目录，**一个文件夹就是一个专家**：

```text
experts/ai-detox-writer/
├── .workbuddy-plugin/plugin.json   专家入口清单
├── agents/ai-detox-writer.md        角色定义、工作流、输出格式、沟通说明
├── avatars/                         头像
├── skills/                          该专家私有的技能（可选）
│   └── rewrite-helper/SKILL.md
└── README.md                        给人看的说明
```

`plugin.json` 的关键字段：`name`（唯一标识，也是缓存 key）、`expertType`（`agent` 单专家 / `team` 专家团）、`agentName`（指向 `agents/` 下的 md 文件名）、`displayName` / `profession` / `displayDescription`（仅用于卡片展示）、`skills[]`（专家自带技能目录）、`runtime`（运行参数）。

**两点对本方案是新信息：**

1. **专家可以自带私有 Skill。** 加载专家时，其私有技能与用户临时选择的技能一起注册进 Toolkit，模型看到的是统一工具集，不需要关心能力来自哪里。本项目当前是单一共享技能根（`AIONUI_SKILLS_DIRS_JSON=[skillsDir]`），没有"专家私有技能"这一层。
2. **目录包 = 可独立安装、分发、复用的资产。** 这比"把专家定义写成 TS 模块"更进一步，也是 3.7 中"资产沉淀"在文件系统层的具体形态。

#### 专家团也是目录包，且刻意自包含

专家团用同样的包结构，靠 `expertType: "team"` 区分：

```text
teams/rd-expert-team/
├── .workbuddy-plugin/plugin.json
│     expertType: "team"
│     teamInfo: { leadAgent, memberAgents[] }
│     members: [{id, role: lead|member}]
│     runtime.workflows: ["需求分析与架构设计", "开发实施与质量保障", "汇总交付"]
├── agents/   主理人 + 每个成员各一个 .md
├── avatars/  有多少成员就有多少头像
├── settings.json   其 agent 必须与 teamInfo.leadAgent 一致
└── README.md
```

作者明确记录了一个考虑很久的取舍：**专家团包内保存成员的完整 Agent Markdown，不依赖对外部专家的引用**。理由是只有这样团队包才能独立安装和分发——换一台机器没装某个外部专家，团队仍然完整。

这是"自包含 vs 引用复用"的经典权衡。对本项目的启示：如果未来要让专家集可分发，**成员定义应当内联而不是引用**；但在第一版内置专家的场景下，引用复用更省事，不必过早自包含。

#### 主理人的行为规则

研发专家团的角色划分（研发交付总监 / 架构师 / 后端 / 前端 / QA）本身是常规的，真正有价值的是主理人的规则：

- **只管调度，不管执行**，不代写任何成员的专业产出；
- **成员之间不直接通信，所有信息流经主理人中转**；
- 任务超出团队能力 → 明确告诉用户做不了；任务信息不够 → 先询问；
- 判断哪些任务可并行、哪些必须等前一步；
- 成员失败、产物冲突或漏项 → 主理人负责补任务和返工；
- **小事不必惊动全队**，问题简单时主理人直接回答。

作者对"成员禁止直连"给出的理由很具体：多一次中转的代价，换来主理人始终知道当前发生了什么，可以在并行任务回传后做冲突检查、在进入下一阶段前补齐上下文；否则会出现"两个成员私下改了方案，最终交付人毫不知情"。

复刻实现中，每个成员的 system prompt 都会被统一追加三条约束：

```text
只完成职责范围内的专业工作；
禁止联系其他成员；
所有结论、风险和产物路径必须用 TeamSay 回传。
```

#### 编排是依赖驱动的任务图，不是固定流程

主理人通过一组团队工具编排——`TeamCreate` / `TaskCreate` / `TaskUpdate` / `AgentCreate` / `TeamSay` / `TeamDelete`——而不是把成员与顺序写死在代码里。原文的说法是：**专家团只给出 SOP 和业务边界，具体让谁干活、拆成几个任务，由主理人根据本轮目标决定。**

任务以显式依赖组织：

```text
任务1  架构设计  owner=architect  depends_on=[]
任务2  后端开发  owner=backend    depends_on=[任务1]
任务3  前端开发  owner=frontend   depends_on=[任务1]
任务4  质量验收  owner=qa         depends_on=[任务2, 任务3]
```

任务 2 与任务 3 无依赖，因此在**各自独立的 Session 中真正并行**，而不是在同一个 Agent 里轮流切换角色。

这修正了本方案原先的一处粗糙设计：我原来按"只读专家可并行、写专家串行"来定并行策略，**实际应当按任务依赖决定并行**。读写属性只影响能否共享产出物，不等同于依赖关系。

#### 三个实现层面的坑（复刻实践，价值最高）

**坑一：定义热更新与缓存不一致。** 改了 `agents/*.md` 但内存里的 Agent 还持有旧 Prompt，页面显示与真实行为就对不上。解法是给专家算版本指纹并计入缓存键：

```text
revision  = sha256({prompt, max_iters, skill_dirs})[:12]
agent_key = f"expert:{id}:{revision}:{model_id}"
```

Prompt、轮次上限、技能目录、连接器、模型中任何一项变化都会导致缓存未命中并重建 Agent。本方案一旦把专家定义变成可配置数据，会遇到完全相同的问题。

**坑二：子 Agent 在工作，用户界面看起来像卡住。** 主理人和每个 Worker 各有独立 Session，前端若只订阅主理人的事件流，成员连续工作几十秒时页面毫无反馈。复刻实现加了一个 `WorkerEventProjector`：检查 Worker Session 的 `team_id`，找到主理人 Session，把 Worker 的原生事件包装后发到主理人的事件通道，再翻译成四类前端已知的消息：

```text
team_member_event  成员开始工作或产生事件
team_member_delta  成员正在增量输出
team_member_done   成员本轮完成
team_tasks         当前任务看板与状态
```

这样前端不必同时管理多条连接，就能在同一个聊天页显示主理人建了哪些任务、调了谁、谁在执行、谁已完成。**这正是本方案 L4 第 5 条"专家活动可见"的实现范式**，而本项目已有 `updateMapper.ts` / `mapUpdateKind` 的更新映射链路可以对齐。

**坑三：一轮对话的"结束"语义被破坏。** 这是最容易被漏掉、也最影响本方案的一条：

> 普通 Agent 收到 `REPLY_END` 这轮对话通常就结束了。专家团不能这样处理，因为主理人的回复结束时，成员可能才刚开始工作。

复刻实现的处理是：收到主理人 `REPLY_END` 后不立即关闭连接，先等状态保存、读取任务看板、检查该 Session 是否仍绑定 Team；只要绑定还在就继续监听，等 Worker 回传或 dispatcher 再次唤醒主理人，直到主理人完成交付并调用 `TeamDelete`，这一轮团队对话才真正结束。

**对本项目的直接影响**：当前 ACP 链路以 `session/prompt` 返回的 `stopReason` 作为一轮结束的标志（`createDshConnection.ts` 的 `prompt()`）。引入专家层后，`stopReason` 只代表主控暂时说完，不代表任务完成。**这是一个本方案此前完全没有预见的技术风险**，且与 C4（研究模式后台长时运行）是同一个问题的两种表现。

#### 专家 vs 专家团的选择判据

| 维度     | 单专家                     | 专家团                                   |
| -------- | -------------------------- | ---------------------------------------- |
| 核心问题 | 一个角色如何稳定交付       | 多个角色如何围绕同一目标协作             |
| 上下文   | 一个 Session               | 主理人 + 多个 Worker Session             |
| 任务拆分 | 在专家自身的推理循环内完成 | 主理人创建结构化任务与依赖               |
| 执行     | 单体执行                   | 有依赖的串行、无依赖的并行               |
| 信息交换 | 无需跨 Agent 通信          | Worker 回传，主理人中转                  |
| 成本     | 较低                       | 多次模型调用、更长运行、更复杂的异常处理 |
| 适用     | 写作、分析、评审、翻译     | 软件交付、复杂调研、内容项目、多角色审查 |

作者给出的判断方法简洁可用：**一位专家能从头做到交付，就交给专家；只有当任务确实存在多种专业责任、拆开后又有清楚的输入输出和依赖时，才用专家团。**

#### 对本方案的六点修正

| 编号 | 吸收项                                                                                          | 影响章节        |
| ---- | ----------------------------------------------------------------------------------------------- | --------------- |
| W6   | 专家字段补 **`outputTemplate`（工作模板）** 与 **`communicationStyle`（沟通风格）**，凑齐五要素 | 5.3             |
| W7   | **成员间禁止直连，信息一律经主控中转**；专家 persona 统一追加三条尾注约束                       | 5.3、7.2        |
| W8   | 并行策略改为**依赖驱动的任务图**，而非按读写属性划分                                            | 7.2             |
| W9   | 专家定义需要**版本指纹**参与运行时缓存键，避免改了定义行为不变                                  | 6 L1、9         |
| W10  | 专家活动可见性采用**事件投射**方案，映射为四类消息，对齐现有 `mapUpdateKind`                    | 6 L4、9         |
| W11  | **`stopReason` 语义脱钩**：主控回合结束 ≠ 任务完成，需引入团队绑定检查再决定是否收流            | 9、10（新风险） |

W11 是本节最重要的发现——它不是设计优化，而是一个会导致"任务还在跑、界面已收尾"的实现级缺陷，必须在专家层落地之前解决。

### 3.9 校准结论：对职能契约的三处强化与一项新增

| 编号 | 强化项                                                                        | 依据                          |
| ---- | ----------------------------------------------------------------------------- | ----------------------------- |
| C1   | 办公模式成功判据补"变更透明"：明确列出改动的文件、单元格与段落                | 3.1 Claude for Excel 变更追溯 |
| C2   | 研究模式成功判据补"引用可达性自检"：交付前校验 URL 可解析，标注失效与存疑来源 | 3.3 引用幻觉实证与缓解效果    |
| C3   | 编码模式成功判据补"证据绑定"：完成声明须附命令与结果，被中断的检查不得算通过  | 3.2 验证不可省略              |
| C4   | 新增能力要求：研究模式需支持后台长时运行与进度可见                            | 3.3 典型 15–30 分钟运行时长   |

这四条已并入第 5 节契约表与第 9 节实施顺序。

## 4. 优化总原则

原则一：**把模式间边界的划分依据从"话题域"改为"交付物形态 + 副作用范围 + 验证方式"三元组。**

原则二：**把模式内职责拆成专家智能体，每个专家单一职责、工具收敛、读写分离。**

原则三：**两级边界都要在四层同时落地，而不是只写在提示词里。**

```text
              模式层（互斥）                  专家层（互补）
L1 Persona    模式主控 persona：定位/交付物/  专家 persona：单一职责/输入输出契约/
              越界移交/汇报要求              允许动作
L2 能力       模式可用的技能与 MCP 全集      每个专家实际加载的工具子集
L3 权限       沙箱根、可写目录（模式上限）    每个专家的读/写/执行权限（不超过模式上限）
L4 认知       i18n 描述、推荐提示词、默认模式  专家活动在时间线可见（不作为选择项）
```

关键约束：**专家层的权限是模式层权限的子集，不能突破模式上限。** 模式层定义"这个会话最多能动什么"，专家层定义"这一步实际需要动什么"。

只做 L1 等于现状；只有四层一致，边界才既被模型遵守、又被系统兜底、还被用户理解。

## 5. 两级职能契约

### 5.1 模式契约（模式之间，互斥）

| 维度         | 办公模式 Office                                                                        | 编码模式 Coding                                                                                          | 研究模式 Research                                                                                                   |
| ------------ | -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 一句话定位   | 把已有信息变成可直接使用的业务文件                                                     | 把意图变成可运行、可验证的代码变更                                                                       | 把开放问题变成有证据支撑的结论                                                                                      |
| 主交付物     | docx / xlsx / pptx / pdf / 成稿文档                                                    | 仓库内代码变更 + 验证记录                                                                                | 带引用的结构化研究结论与证据清单                                                                                    |
| 成功判据     | 文件可直接使用、格式正确、忠实于素材；**变更透明：列出改动的文件、单元格与段落**（C1） | 变更最小、检查与测试通过、风险已说明；**证据绑定：完成声明须附命令与结果，被中断的检查不得算通过**（C3） | 每条主张可溯源、来源权威且注明时间、冲突已呈现；**引用可达性自检：交付前校验 URL 可解析，标注失效与存疑来源**（C2） |
| 主要输入     | 用户提供的素材、数据、研究模式的产出                                                   | 仓库代码与工程上下文                                                                                     | 外部网络来源 + 用户提供的材料                                                                                       |
| 主要副作用   | 向交付目录写文件                                                                       | 改写仓库、执行构建与测试                                                                                 | 以只读检索为主，仅向研究输出目录写入                                                                                |
| 运行形态     | 交互式，秒级到分钟级                                                                   | 交互式，含审批中断                                                                                       | **后台长时运行，进度可见**（C4）                                                                                    |
| 明确不做     | 不做开放式网络取证；不做仓库工程改造                                                   | 不做面向业务的成稿排版与美化                                                                             | 不产出最终排版文件；不改动业务代码                                                                                  |
| 越界时的动作 | 说明归属并建议移交研究 / 编码                                                          | 说明归属并建议移交办公                                                                                   | 说明归属并建议移交办公 / 编码                                                                                       |

三条去重叠的关键决策：

1. **联网取证与多源交叉验证归研究模式独占。** 办公 persona 删除 `research` 表述，改为"处理用户已提供或由研究模式产出的素材；需要开放调研时建议转研究模式"。
2. **生成 Office 成品文件归办公模式独占。** 研究模式产出 Markdown / 结构化数据 + 引用清单，需要成稿时建议移交办公模式。
3. **仓库工程变更归编码模式独占。** 办公与研究模式允许写一次性数据处理脚本，但不改动用户仓库源码 —— 这条由 L3 的沙箱根落地，而不只是提示词约定。

注意"建议移交"不是硬禁止。用户在会话中明确坚持时，模型仍应完成任务，只在首次越界时说明一次。硬禁止会把契约变成用户体验事故。

### 5.2 专家契约（模式之内，互补）

每个模式由一个**模式主控**（承接用户对话、规划、委派、汇总）加若干**专家智能体**组成。专家不直接与用户对话，由主控按需委派。

#### 办公模式的专家集

| 专家     | 唯一目标                       | 读/写       | 产出契约                       |
| -------- | ------------------------------ | ----------- | ------------------------------ |
| 素材整理 | 把原始素材归一为结构化中间数据 | 读+写中间件 | 清洗后的数据集与素材清单       |
| 文档撰写 | 长文档结构与成稿               | 写          | docx / md，附变更清单          |
| 表格构建 | 数据建模、公式与多表工作簿     | 写          | xlsx，保持公式依赖完整         |
| 演示构建 | 叙事结构与幻灯片               | 写          | pptx，附页级大纲               |
| 校对审查 | 事实一致性、格式规范、术语统一 | **只读**    | 问题清单与修改建议，不直接改稿 |

#### 编码模式的专家集

| 专家     | 唯一目标                     | 读/写       | 产出契约                     |
| -------- | ---------------------------- | ----------- | ---------------------------- |
| 仓库勘察 | 定位代码路径、依赖与既有约定 | **只读**    | 结论与关键文件行号，不改代码 |
| 变更实现 | 实施单一意图的最小变更       | 写          | 一个 commit 量级的 diff      |
| 验证执行 | 运行测试、类型检查、lint     | 只读 + 执行 | 命令与原始结果，不改代码     |
| 变更审查 | 以新鲜上下文审查 diff        | **只读**    | 问题清单与严重度             |

"变更实现"与"变更审查"必须是不同专家：新鲜上下文的审查质量更高，模型不会偏袒自己刚写的代码（见 3.2）。"验证执行"与"变更实现"分离，则是"读写分离"在工程场景的直接落地（见 3.6）。

#### 研究模式的专家集

| 专家     | 唯一目标                         | 读/写    | 产出契约                     |
| -------- | -------------------------------- | -------- | ---------------------------- |
| 研究规划 | 澄清问题与决策标准，产出研究计划 | 只读     | 研究计划与子问题分解         |
| 资料检索 | 按子问题检索（**可并行多实例**） | **只读** | 来源清单 + 摘录 + 检索词记录 |
| 证据核查 | 交叉验证主张、校验 URL 存活      | **只读** | 核验结论、失效与存疑来源标注 |
| 综述撰写 | 结构化综述与引用落点             | 写       | 带引用的报告，引用落到主张处 |

这正是深度研究的标准多智能体架构——规划、并行探索、结构化综述由专门子智能体分担（见 3.3）。"证据核查"独立成专家，是 C2 引用可达性自检的执行者。

### 5.3 专家的规范定义字段

每个专家必须用统一字段声明，用于生成 persona、配置工具与权限，并作为评测依据（字段取自 3.4 的角色 taxonomy 建议）：

```text
id                 稳定标识（也是缓存 key，禁止用中文名或临时编造的标识）
mode               所属模式
goal               唯一目标（一句话，结果导向，不得与同模式其他专家重叠）
method             方法论：该角色按什么步骤工作、先做什么后做什么        ← W1
input              输入契约
output             输出契约（结构化优先）
outputTemplate     工作模板：常见任务的输出格式骨架                     ← W6
communicationStyle 沟通风格：要点式还是详述、是否用表格                 ← W6
decisionScope      决策范围（能自行决定什么，什么必须回主控）
allowedTools       允许工具清单（默认最小集，显式配置）
ownSkills          该专家私有的技能（可选，与全局技能一起注册）          ← 3.8
access             由 allowedTools 推导的 read | write | execute，沙箱据此兜底  ← W2
parallelizable     是否可并行多实例
```

这组字段对齐 WorkBuddy 的专家五要素（3.8）：`goal` + `decisionScope` 是人设，`method` 是方法论，`allowedTools` + `ownSkills` 是工具链，`outputTemplate` 是工作模板，`communicationStyle` 是沟通风格。原设计缺的是后三者——**只说"你是谁、你能用什么"而不说"你怎么做、交付长什么样"，专家每次都会靠模型临场发挥，行为稳定性无从谈起。**

所有专家 persona 统一追加三条尾注约束（W7，取自 3.8 的成员模板）：

```text
只完成职责范围内的专业工作；
禁止联系其他专家；
所有结论、风险和产物路径必须回传给模式主控。
```

四条硬规则：

1. **同一模式内任意两个专家的 `goal` 不得重叠**，上线前用 dry simulation 校验（3.4）。
2. **`allowedTools` 必须显式声明**。DSH 子智能体获得全新扁平注册作用域、不继承父的工具限制（1.3），因此"不配置"等于"拿不到工具"或"拿到默认全集"，两种都是错误状态。
3. **权限跟随工具，沙箱兜底**（W2）。`access` 不作为独立配置项，而是从 `allowedTools` 推导：一个只挂了读取与搜索工具的专家自然就是只读的。沙箱按推导结果做上限校验，防止工具清单与权限意图不一致。这采纳了 WorkBuddy"专家本身不获取系统权限、实际动作靠挂载的 Skill / MCP"的模型——**角色只决定行为倾向，权限由工具边界决定**，比让人手工维护两份可能冲突的配置更可靠。
4. **专家之间禁止直接通信**（W7）。所有结论与产物路径回传主控，由主控中转。多这一跳的代价，换来主控始终掌握全局：可以在并行任务回传后做冲突检查、在进入下一阶段前补齐上下文，避免"两个专家私下改了方案、最终交付者毫不知情"（3.8）。

## 6. 分层落地设计

### L1 Persona：模式主控与专家两套

**模式主控 persona**（`personaForDshWorkMode()` 改写）采用五段式：

```text
1. 角色与定位（含 {{cwd}}）
2. 交付物契约：主产出物形态、成功判据
3. 边界与移交：明确不做什么；越界时先说明归属并给出建议动作，用户坚持则继续执行
4. 委派规则：本模式有哪些专家、何时委派、何时自己做、如何汇总专家产出
5. 汇报要求：产出物位置、验证状态、剩余风险 / 证据与不确定性
```

第 3 段是模式之间唯一能互相"指认"的机制，第 4 段是专家层的入口，两段当前都不存在。

第 4 段的委派判据借鉴 WorkBuddy 的选型速查（3.7 / W4），写成三条可判定的规则：

```text
需要的只是一种工具能力          → 直接调用工具/技能，不委派
是一个明确的单点问题            → 委派单个专家
任务复杂、需要多角色分工协作    → 委派多专家（只读类可并行）
```

**专家 persona** 由 5.3 的规范字段自动生成，保持短小（单一职责、方法论步骤、输入输出契约、允许动作、回传格式），不重复模式主控的内容——每增加一项无关职责都在稀释注意力（3.4）。

建议把 persona 抽到独立模块，例如：

```text
packages/dsh-bridge/src/personas/
  modes/{office,coding,research}.ts    模式主控
  experts/{office,coding,research}/*.ts 专家定义（5.3 字段）
  buildExpertPersona.ts                 由字段生成专家 persona
  expertRevision.ts                     由定义计算版本指纹
```

**版本指纹（W9）。** 专家定义一旦成为可配置数据，就会遇到"改了定义但运行时还拿着旧 persona"的问题——界面显示与真实行为对不上，且极难排查。参照 3.8 的做法，由定义内容计算指纹并计入子智能体的复用键：

```text
revision = sha256({ persona, allowedTools, ownSkills, maxTurns })[:12]
key      = `expert:${id}:${revision}:${modelId}`
```

persona、工具清单、私有技能、轮次上限、模型任一变化都应导致重建，而不是命中旧实例。

关于是否采用 WorkBuddy 的**目录包**形态（3.8）：第一版建议仍用 TS 模块，因为内置专家不需要独立分发，目录包会引入清单解析、校验与版本管理的额外成本。但字段设计应当与目录包保持同构，使未来导出为可分发资产时无需重构。

避免继续在一个函数里堆字符串，也让"专家清单"成为可枚举、可测试的数据而不是散落的提示词。

这一点还有更远的意义（W5）：WorkBuddy 把专家做成了**可沉淀、可复用、可分发的资产**——项目空间里调试好的专家、Skill 与连接器可供多人复用，企业版还支持上传岗位说明书生成专家（3.7）。把专家定义从提示词升格为结构化数据，是通向这条路径的前提。但**第一版不实现用户自定义专家**，先把内置专家集做稳；自然语言造专家（对应 WorkBuddy 的 `expert-manager`）列为后续演进。

### L2 能力层：按模式的默认技能与推理档位

把 `defaultAssistantConfig(defaultModelId)` 扩展为 `defaultAssistantConfigForMode(mode, defaultModelId)`，填入模式化默认值：

| 模式     | defaults.skills                 | defaults.mcps | thought_level  | disabled_builtin_skills    |
| -------- | ------------------------------- | ------------- | -------------- | -------------------------- |
| office   | fixed：officecli 等文档处理技能 | fixed: []     | 偏低（响应快） | 关闭仓库 / 代码类内置技能  |
| coding   | fixed：代码检索、测试分析类技能 | auto          | 中             | 无                         |
| research | fixed：检索与引用整理类技能     | auto          | 高             | 关闭 Office 成品生成类技能 |

同时利用 DSH patch 已支持的内联 JS 表达式，按模式条件化插件行。启动连接时额外注入 `AIONUI_DSH_MODE=<mode>`，patch 内即可写：

```yaml
- id: web-search-deepseek
  disabled: !!js process.env.AIONUI_DSH_MODE === 'office'
```

这条是成本最低、收益最直接的一处改动：不需要改 DSH 源码，也不需要新增插件包。

依赖提示：研究模式强依赖 `DEEPSEEK_SEARCH_API_KEY`。未配置时应在 UI 上把研究模式标为"需配置"，而不是静默降级成一个没有检索能力的普通对话。

**专家层的工具收敛。** 上表定义的是"模式可用的工具全集"，每个专家实际加载的是其中一个子集，通过子智能体创建时的工具限制配置。这一步不是可选优化——工具规模上升会显著劣化选择准确率（3.5 引用的数据：50 个工具约 94%，200 个降到 64%），而目的重叠的工具会主动干扰智能体选择高效策略（3.4）。举例：编码模式的"仓库勘察"专家只需读文件与搜索工具，不应持有写文件与执行命令的工具。

### L3 权限层：模式设上限，专家按需收敛

`@deepseek-ai/dsh-sandbox` 已确认支持 `read-only` / `workspace-write` / `danger-full-access` 三种模式，并支持 `workspaceRoot` 与 `writableRoots`。据此把 patch 中写死的沙箱与审批改为按 env 解析：

```yaml
- id: sandbox-policy
  config:
    mode: !!js process.env.AIONUI_DSH_SANDBOX_MODE || 'workspace-write'
    workspaceRoot: !!js process.env.AIONUI_DSH_WORKSPACE_ROOT || process.cwd()
    writableRoots: !!js JSON.parse(process.env.AIONUI_DSH_WRITABLE_ROOTS_JSON || '[]')
```

目标策略：

| 模式     | sandbox mode    | workspaceRoot | writableRoots                         | approval |
| -------- | --------------- | ------------- | ------------------------------------- | -------- |
| office   | workspace-write | 工作区根      | 交付目录                              | ask      |
| coding   | workspace-write | 项目根        | -                                     | ask      |
| research | read-only       | 工作区根      | 研究输出目录（如 `research-output/`） | ask      |

两点必须先验证再实施：

1. `approval` 插件支持的 policy 取值（当前只用过 `ask`），未验证前不要引入按模式的审批差异。
2. `read-only` + `writableRoots` 是否可组合。若不支持，研究模式退化为 `workspace-write` 且 `workspaceRoot` 指向研究输出目录，素材以只读方式另行提供。

这一层是唯一能把"办公模式不改仓库代码"从建议变成保证的地方。

**专家层的权限收敛。** 上表是模式上限，专家在上限之内按其 `allowedTools` 推导出的 `access` 进一步收窄（W2：权限跟随工具，沙箱兜底）：

| access 类型 | 专家举例                                         | 策略                         |
| ----------- | ------------------------------------------------ | ---------------------------- |
| 只读        | 仓库勘察、变更审查、资料检索、证据核查、校对审查 | 沙箱 `read-only`，无写工具   |
| 只读 + 执行 | 验证执行                                         | 可跑测试命令，但无写文件工具 |
| 写          | 变更实现、文档撰写、表格构建、演示构建、综述撰写 | 继承模式的可写目录，不得扩大 |

这正是 3.6 的"按离散任务建模角色 + 分离读与写"在本项目的落地形式，比按模式建模更精确：**同一个模式里，大多数步骤其实不需要写权限。**

沙箱在这里的角色是**兜底而非主配置**：正常情况下只读专家因为没有写工具而写不了，沙箱拒绝事件应当为零；一旦出现非零，说明工具清单与权限意图不一致，属于配置缺陷而不是运行时正常现象。这也让 8 节的"专家越权率"成为一个有意义的客观指标。

安全注意点：DSH 子智能体会继承父的 sandbox override 与 `'never'` 审批钉选（1.3）。因此**审批必须在模式主控层统一收口**——不得为了让专家链路跑顺而给主控钉 `'never'`，否则写类专家的全部副作用都会绕过审批。这是"环境权限"问题在本架构中的具体形态（3.6）。

### L4 认知层：让边界对用户可见

1. `#workModeName` / `#workModeDescription` 的文案改为返回 i18n key，由前端解析。新增 `agentMode.work.officeDescription` / `codingDescription` / `researchDescription`，并补齐 `i18n-config.json` 列出的全部语言。
2. 前端 pill 增加悬浮说明，展示"擅长 / 不适合"两行，直接复用上表的契约文案。
3. 为每个模式提供 3 条 `prompts.recommended`（当前为空数组），用具体任务示范边界，例如：
   - 办公：把这批会议记录整理成一份纪要文档
   - 编码：定位这个报错并提交最小修复
   - 研究：对比三家供应商方案并给出带来源的选型建议
4. 默认模式改为上下文感知：所选工作区包含 `.git` 或常见工程清单文件时默认 coding，否则默认 office；`prewarmMode` 跟随同一判定，避免预热错进程。用户显式选择仍优先于推断结果，并继续通过 `guid.lastAssistantId` 持久化。
5. **专家活动可见但不可选。** 用户的一级选择仍然只有三个模式按钮，专家不进入选择器——否则等于把内部编排的复杂度推给用户（WorkBuddy 同样明确"用户不需要配置工作流，也不需要知道底层跑了几个智能体"）。但专家活动必须可观察，否则用户无法理解结果从何而来，也无法判断该信任到什么程度。

   实现上采用 3.8 的**事件投射**方案（W10）：专家在各自的子会话中运行，其事件需被投射到主控的事件通道，再映射为四类前端消息——

   ```text
   expert_started   某专家开始工作（"『仓库勘察』正在检索相关实现"）
   expert_delta     专家增量输出
   expert_done      专家本轮完成（"『证据核查』校验 12 条引用，2 条失效"）
   expert_tasks     当前任务图与状态看板
   ```

   若不做投射，专家连续工作几十秒时界面会完全静默，看起来像卡死。本项目已有 `updateMapper.ts` / `mapUpdateKind` 的更新映射链路，新增这四类 update kind 即可对齐，无需新建前端连接。

## 7. 两种协作机制：模式间移交与模式内委派

两级模型对应两种截然不同的协作语义，不能混用——这正是外部范式里 handoff 与 manager 的分野（3.5）：

| 机制 | 模式间移交 handoff         | 模式内委派 delegation          |
| ---- | -------------------------- | ------------------------------ |
| 语义 | 控制权转移，接手方接管对话 | 中心化编排，主控保留对话控制权 |
| 范围 | 跨会话（新建目标模式会话） | 同一会话内                     |
| 触发 | 越界，任务属于另一个模式   | 任务需要本模式某个专家的能力   |
| 用户 | 显式确认（点击卡片）       | 无需介入，仅可观察             |
| 载体 | 移交摘要 + 会话关联        | DSH 子智能体（spawn / fork）   |

### 7.1 模式间移交（handoff）

保留"模式是会话级不可变身份"这条已确立的设计，新增**移交**而非中途切换：

```text
研究会话 --产出结论与证据--> [建议移交办公] --> 新建办公会话（同 workspace）
                                              首条消息注入移交摘要
```

设计要点：

- 模型在 persona 第 3 段识别越界后，输出一段带标记的移交建议；前端渲染为可点击卡片。
- 点击后在**同一 workspace / 项目**下创建目标模式的新会话，把移交摘要（原始目标、已产出文件路径、关键结论）作为首条消息注入。
- 会话记录 `extra.handoff_from_conversation_id`，侧边栏做关联展示，使跨模式任务在 UI 上仍是一条链路。

这样既不破坏"模式不可变"的一致性保证，又解决了 P6 的上下文丢失问题。外部 handoff 范式的两个细节值得吸收：**接手方必须拿到足够历史**（此处由移交摘要承担），**移交事件要可观测**（用于第 8 节度量）。

### 7.2 模式内委派（delegation）

由模式主控通过 DSH 子智能体机制调度本模式的专家：

```text
用户 ──→ 模式主控（保留对话控制权）
            ├─→ 专家 A（只读）──→ 结构化结论
            ├─→ 专家 B（只读，可并行多实例）──→ 结构化结论
            └─→ 专家 C（写）──→ 产出物 + 变更清单
         主控汇总后统一回复用户
```

这与 WorkBuddy 的「团长 + 并行子专家」编排同构（3.7）：团长拆解任务、分配给不同专家并行执行、合并输出，而用户不需要配置工作流，也不需要知道底层跑了几个智能体。

**委派是升级路径，不是默认行为（W3）。** WorkBuddy 披露的成本尺度是专家团模式消耗为单专家的 **3–5 倍**，这个量级足以说明：对简单任务无脑委派是净损失。主控应按 L1 第 4 段的三条判据决定——只需要一种工具能力就直接调用，单点问题派单个专家，只有真正需要多角色分工时才展开专家团。3.8 给出的判断更简洁，可直接作为主控的自问：**一位专家能从头做到交付就不要建队；只有任务确实含多种专业责任、拆开后有清楚的输入输出和依赖，才展开。**

主控的行为规则取自 3.8 的主理人契约，写入 L1 第 4 段：

```text
只管调度，不代写专家的专业产出；
任务超出本模式能力 → 明确说明并建议移交（走 7.1）；
任务信息不足 → 先询问，不要凭猜测开工；
按依赖拆分任务，判断何者可并行；
专家失败、产物冲突或漏项 → 由主控补派任务或要求返工；
简单问题直接回答，不惊动专家层。
```

设计要点：

- **主控不直接干重活**，它负责澄清、规划、委派、汇总与对外汇报；专家不与用户直接对话。
- **委派请求与回传都用结构化输出**，避免专家产出被当作自由文本再解析一遍。
- **专家之间不直接通信**，一律经主控中转（W7）。
- **并行由任务依赖决定，不由读写属性决定**（W8）。主控把本轮工作拆成带 `owner` 与 `depends_on` 的任务图，无依赖的任务并行、有依赖的串行。以研究模式为例：

  ```text
  任务1  研究规划   owner=planner     depends_on=[]
  任务2  子问题A检索 owner=searcher    depends_on=[任务1]
  任务3  子问题B检索 owner=searcher    depends_on=[任务1]
  任务4  证据核查   owner=verifier    depends_on=[任务2, 任务3]
  任务5  综述撰写   owner=synthesizer depends_on=[任务4]
  ```

  任务 2、3 真正并行在各自的子智能体中执行，而不是在同一个上下文里轮流切换角色。读写属性只影响能否共享产出物，不等于依赖关系——这是 3.8 修正的原设计缺陷。

- **深度限制**：专家不得再派生下一级专家，即 `maxDepth = 1`。第一版不需要多层编排，且能防止失控递归。
- **失败语义**：单个专家失败应回到主控并降级处理（说明缺失了哪一步），而不是让整轮任务失败。

第一版不必上全套专家。建议每个模式先落 2–3 个，优先选**收益最确定的读写分离对**：编码模式先上"仓库勘察（只读）+ 变更审查（只读）"，研究模式先上"资料检索（可并行）+ 证据核查"，办公模式先上"素材整理 + 校对审查（只读）"。其余专家按实测效果再补——智能体更多不等于设计更好（3.4）。

## 8. 边界度量

建立一个模式归属评测集，约 30 条提示，分三类：

- 明确属于某模式（每模式 5 条）
- 明确跨界（10 条）
- 表述模糊（5 条）

指标：

| 指标         | 定义                                                                       |
| ------------ | -------------------------------------------------------------------------- |
| 归属正确率   | 在本模式内正确完成，或正确识别并建议移交                                   |
| 越界率       | 办公模式直接改仓库源码 / 研究模式直接产出 pptx 等                          |
| 契约符合率   | 研究模式每条主张是否带引用；编码模式是否报告验证状态；办公模式是否列出变更 |
| 引用可解析率 | 研究模式产出的引用 URL 中可正常解析的比例（自动化可测，见下）              |
| 移交有效率   | 移交后的新会话是否无需用户重述上下文即可继续                               |
| 委派正确率   | 主控是否委派给了正确的专家、是否漏派该走的专家、是否越权自己动手           |
| 专家越权率   | 只读专家是否尝试写入、写专家是否超出其可写目录                             |

度量同样分两级：**模式层**看归属正确率与越界率（用户选得对不对、模式守不守界），**专家层**看委派正确率与专家越权率（模式内部分工是否成立）。专家越权率应由沙箱拒绝事件直接统计，属于客观指标，不依赖人工判读。

三点方法论要求：

1. **过程级而非仅终稿。** 端到端评测会掩盖累积于中间步骤的幻觉，研究模式应审计 plan–search–summarize 全轨迹（[arXiv 2601.22984](https://arxiv.org/abs/2601.22984)）。
2. **引用可解析率可自动化。** 用 Wayback Machine 区分"链接失效"与"凭空捏造"，这是唯一一条可无人值守跑的指标，也是 C2 的验收手段（[arXiv 2604.03173](https://arxiv.org/abs/2604.03173)）。
3. **路由失败模式要显式覆盖。** 评测集必须包含含糊表述、一条消息含多个诉求（关键是不能静默丢弃其一）、以及处于模式边缘的输入（[Microsoft AI Agent Eval Scenario Library](https://github.com/microsoft/ai-agent-eval-scenario-library/blob/main/business-problem-scenarios/triage-and-routing.md)）。

除引用可解析率外不进 CI（成本与不确定性都不适合），作为发版前的半自动检查，并在每次 persona 改动后重跑。移交事件应落埋点（对应 handoff 范式的 `on_handoff` 钩子），使移交有效率可统计。

## 9. 实施顺序

| 阶段 | 内容                                                                                                                     | 风险 | 依赖                                                |
| ---- | ------------------------------------------------------------------------------------------------------------------------ | ---- | --------------------------------------------------- |
| 1    | 模式主控 persona 五段式重写（含 C1 变更透明、C3 证据绑定）+ 移交规则文本 + 描述 i18n + 推荐提示词 + 上下文感知默认模式   | 低   | 无                                                  |
| 2    | `defaultAssistantConfigForMode`：按模式默认 skills / mcps / thought_level                                                | 低   | 技能包就位                                          |
| 3    | 注入 `AIONUI_DSH_MODE`，patch 按模式条件化插件行                                                                         | 中   | 阶段 2                                              |
| 4    | C2 引用可达性自检：研究模式的 URL 存活校验技能 + 交付前自检要求                                                          | 低   | 阶段 2                                              |
| 5    | **回合语义改造（W11 前置）**：主控 `stopReason` 不再等同任务完成，引入"是否仍有在途专家"的判定后再决定收流               | 高   | 阶段 1；必须先于专家层落地                          |
| 6    | **专家层地基**：5.3 规范字段建模 + `buildExpertPersona` + 版本指纹（W9）+ 每模式 2–3 个最小专家集 + 子智能体委派链路打通 | 中高 | 阶段 5；验证 DSH subagent 插件是否在 acp profile 中 |
| 7    | **专家层工具与权限收敛**：`allowedTools` 显式化 + 由其推导 `access` + 沙箱兜底校验                                       | 中高 | 阶段 6；先验证 sandbox / approval 取值              |
| 8    | 按模式的沙箱根与可写目录（模式上限）                                                                                     | 中高 | 与阶段 7 同批验证                                   |
| 9    | 专家活动可见：事件投射 + 四类 update kind（W10，L4 第 5 条）                                                             | 中   | 阶段 5、6；现有 `mapUpdateKind` 链路                |
| 10   | 移交机制（后端 extra 字段 + 前端卡片 + 侧边栏关联 + 移交埋点）                                                           | 中   | 阶段 1                                              |
| 11   | C4 研究模式后台长时运行与进度可见                                                                                        | 中   | 阶段 5（同一问题域）                                |
| 12   | 两级边界评测集与回归基线（含引用可解析率与专家越权率自动化）                                                             | 低   | 阶段 1、4、7、10                                    |

三条顺序说明：

1. 阶段 1 重写主控 persona 时，第 4 段「委派规则」先只落"何时直接调用工具 / 何时该由专家处理"的判据文本（W4），专家清单留空；等阶段 6 专家集落地后再填入具体专家。这样阶段 1 仍然独立可交付，不会依赖尚不存在的专家层。
2. **阶段 5 必须先于阶段 6**。回合语义未改造就上专家层，会直接产生"任务仍在跑、界面已收尾"的缺陷（W11），且这种问题一旦混在专家层的调试里极难定位。
3. 阶段 6 之前必须确认：**`dsh-subagent-spawn-in-process` / `dsh-subagent-fork-in-process` 是否包含在当前使用的 `acp` profile 中**。node_modules 里只装了共享驱动库 `dsh-subagent-in-process-driver`，provider 后端由 profile 通过 pnpm 安装在 `DSH_HOME` 下。若 acp profile 未包含，需要按 [DSH_PLUGIN_GUIDE.md](./DSH_PLUGIN_GUIDE.md) 的 patch `insert` 机制补装——这仍然不需要改 DSH 源码。

阶段 1 独立可交付，且已经能消除 P1、P4、P5 三个最影响体验的问题，建议先合入验证效果再推进后续阶段。阶段 5–7 是专家层的主体工作量，也是解决 P8 的关键，建议作为第二个独立里程碑推进，不要与阶段 1 混在同一批变更里。

## 10. 风险与取舍

- **进程内存**：三模式各占一个常驻 DSH 进程，叠加模式化技能后开销更高；专家层是进程内子智能体，不新增进程，但会增加单进程内存与并发负载。建议为 `DshRuntimePool` 增加空闲回收（某模式在 N 分钟内无活跃会话则 dispose 其 bridge），并对并行只读专家设并发上限。
- **专家层过度拆分**：这是本方案最容易走偏的地方。智能体更多不等于设计更好，目的重叠的角色与工具会主动干扰选择（3.4）。硬约束是 5.3 的"同模式内 `goal` 不得重叠"，工程约束是第一版每模式只上 2–3 个专家，靠 8 节的委派正确率决定是否再加。
- **审批被专家链路绕过**：DSH 子智能体继承父的 `'never'` 审批钉选（1.3）。若为了让委派跑顺而给主控钉 `'never'`，写类专家的全部副作用将不再经过用户确认。审批必须在主控层统一收口，这一条应作为代码评审的硬检查项。
- **延迟与成本**：委派会增加轮次与 token 消耗。可参照的量级是 WorkBuddy 专家团模式约为单专家的 **3–5 倍**（3.7），研究模式的并行检索还会更高。对简单任务主控应直接完成而不委派，这条要写进主控 persona 的委派规则段，并纳入发版前的成本回归观察。
- **回合语义脱钩（W11，最高优先级）**：当前一轮对话以 `session/prompt` 返回的 `stopReason` 为终点（`createDshConnection.ts`）。引入专家层后，主控回合结束时专家可能才刚开始工作，若沿用现有语义，会出现"任务仍在跑、界面已收尾"——流被关闭、后续专家回传无处可去。必须在专家层落地**之前**改造：收到主控 `stopReason` 后先检查是否仍有在途专家，有则继续监听，直至主控完成汇总交付。这与 C4（研究模式后台长时运行）是同一问题的两种表现，应合并处理。
- **专家定义热更新失效（W9）**：改了专家定义但运行时仍持有旧 persona，界面与行为不一致且极难排查。由定义内容计算版本指纹并计入复用键可解决，成本很低，但必须在专家层第一版就做，否则后续每次调参都会踩。
- **专家资产化的范围蔓延**：把专家做成可配置资产之后，很容易滑向"用户自定义专家 / 自然语言造专家"（WorkBuddy 的 `expert-manager`）。第一版明确不做，只提供内置专家集；否则边界治理问题会从"三个模式"扩散到"用户可造的任意角色"，而本方案要解决的恰恰是边界失控。
- **收紧沙箱带来的新摩擦**：研究模式改只读后会新增"权限被拒"路径，必须配套清晰的错误文案和一键放宽入口，否则是体验倒退。
- **契约过硬**：把"不做"实现为硬拒绝会激怒用户。统一采用"首次越界说明一次 + 用户坚持则执行"的软边界。
- **旧会话兼容**：模式化默认配置只作用于新建会话；已存在的会话继续使用其 `capability_snapshot`，与现有"快照不随全局配置变化"的设计保持一致。
- **是否需要第四个模式**：不建议。当前痛点是三者重叠而非覆盖不足，新增模式只会让选择更难。需要新增能力时，优先考虑给某个模式加一个专家，而不是加一个模式——这正是两级模型带来的扩展空间。

## 11. 结论

现在的三模式解决了"按钮不只是改文字"的问题，但止步于行为倾向层，而且把"模式"实现成了"一个智能体"。

要让它成为真正的能力边界，需要在两个层级同时建模：

- **模式间**：把划分依据从话题域换成交付物、副作用与验证方式，让三个模式互斥且各自可判定，越界时走移交而不是硬扛。
- **模式内**：把单一智能体拆成职责单一、工具收敛、读写分离的专家集，由模式主控委派与汇总。

两级边界都必须在 persona、能力、权限、界面四层一致表达，并有对应的度量。

有一件事要排在设计之前：**回合语义改造（W11）**。现在"一轮对话结束"等同于主控说完话，而专家层成立的前提恰恰是"主控说完了但任务没完"。这不是优化项，是专家层能否成立的地基。

当前状态是：模式间边界只写在提示词里，模式内边界根本不存在。在补上这两者之前，**三个模式在系统看来仍然是同一个智能体。**

## 12. 参考资料

### 办公场景

- [Create files with Word, Excel, and PowerPoint Agents in Microsoft Copilot — Microsoft Learn](https://learn.microsoft.com/en-us/microsoft-365/copilot/wordexcelppt-agents)
- [Introducing Word, Excel, and PowerPoint Agents in Microsoft 365 Copilot — Microsoft Community Hub](https://techcommunity.microsoft.com/blog/microsoft365copilotblog/introducing-word-excel-and-powerpoint-agents-in-microsoft-365-copilot/4470604)
- [Advancing Claude for Financial Services — Anthropic](https://www.anthropic.com/news/advancing-claude-for-financial-services)
- [Anthropic's Claude is learning Excel so you don't have to — The Register](https://www.theregister.com/2025/10/28/anthropic_claude_excel/)

### 编码场景

- [Best practices for Claude Code — Claude Code Docs](https://code.claude.com/docs/en/best-practices)
- [10 Claude Code Best Practices for Agentic Coding — OpenHands](https://www.openhands.dev/blog/claude-code-best-practices-agentic-coding)
- [AGENTS.md for Codex and AI Coding Agents — BSWEN](https://docs.bswen.com/blog/2026-08-24-how-to-write-agents-md/)

### 研究场景

- [Gemini Deep Research agent — Gemini API 文档](https://ai.google.dev/gemini-api/docs/deep-research)
- [Build with Gemini Deep Research — Google 开发者博客](https://blog.google/technology/developers/deep-research-agent-gemini-api/)
- [How OpenAI, Gemini, and Claude Use Agents to Power Deep Research — ByteByteGo](https://blog.bytebytego.com/p/how-openai-gemini-and-claude-use)
- [ChatGPT Deep Research: Guide to AI Agents & RAG — IntuitionLabs](https://intuitionlabs.ai/articles/chatgpt-deep-research-guide-ai-agents-rag)
- [Detecting and Correcting Reference Hallucinations in Commercial LLMs and Deep Research Agents — arXiv 2604.03173](https://arxiv.org/abs/2604.03173)
- [Cited but Not Verified: Parsing and Evaluating Source Attribution in LLM Deep Research Agents — arXiv 2605.06635](https://arxiv.org/pdf/2605.06635)
- [ReportBench: Evaluating Deep Research Agents via Academic Survey Tasks — arXiv 2508.15804](https://arxiv.org/pdf/2508.15804)
- [Why Your Deep Research Agent Fails? On Hallucination Evaluation in Full Research Trajectory — arXiv 2601.22984](https://arxiv.org/abs/2601.22984)

### 边界方法论与多智能体设计

- [Anti-Patterns in Multi-Agent Gen AI Solutions — Arman Kamran](https://medium.com/@armankamran/anti-patterns-in-multi-agent-gen-ai-solutions-enterprise-pitfalls-and-best-practices-ea39118f3b70)
- [AI Agent Anti-Patterns (Part 1): Architectural Pitfalls — Allen Chan](https://achan2013.medium.com/ai-agent-anti-patterns-part-1-architectural-pitfalls-that-break-enterprise-agents-before-they-32d211dded43)
- [AI Agent Anti-Patterns (Part 5): The Illusion of Control — Allen Chan](https://achan2013.medium.com/agent-anti-patterns-part-5-05da1c3c1828)
- [A Practical Guide for Designing, Developing, and Deploying Production-Grade Agentic AI Workflows — arXiv 2512.08769](https://arxiv.org/pdf/2512.08769)
- [Building AI Agents? Here Are Some Anti-Patterns to Avoid — MachineLearningMastery](https://machinelearningmastery.com/building-ai-agents-here-are-some-anti-patterns-to-avoid/)
- [Choose a design pattern for your agentic AI system — Google Cloud 架构中心](https://docs.cloud.google.com/architecture/choose-design-pattern-agentic-ai-system)

### 移交与路由

- [Handoffs — OpenAI Agents SDK](https://openai.github.io/openai-agents-python/handoffs/)
- [Agent orchestration — OpenAI Agents SDK](https://openai.github.io/openai-agents-python/multi_agent/)
- [The Intent Classification Layer Most Agent Routers Skip — tianpan.co](https://tianpan.co/blog/2026/04/16/intent-classification-agent-routers)
- [AI Agent Routing Patterns — Taskade](https://www.taskade.com/blog/ai-agent-routing-patterns)
- [Triage and Routing — Microsoft AI Agent Eval Scenario Library](https://github.com/microsoft/ai-agent-eval-scenario-library/blob/main/business-problem-scenarios/triage-and-routing.md)

### 权限与沙箱

- [Least privilege for AI agents: Identity, access, and tool binding — Microsoft Security Blog, 2026-07](https://www.microsoft.com/en-us/security/blog/2026/07/16/least-privilege-for-ai-agents-identity-access-and-tool-binding/)
- [AI Sandbox: The Complete Guide to Sandboxing AI Agents in 2026 — Cosmonic](https://cosmonic.com/blog/ai-sandbox-guide/)
- [How to sandbox AI agents in 2026: MicroVMs, gVisor & isolation strategies — Northflank](https://northflank.com/blog/how-to-sandbox-ai-agents)
- [How to Apply Least Privilege to AI Agents — Arthur](https://www.arthur.ai/column/ow-to-apply-least-privilege-to-ai-agents)
- [What permissions should an AI agent have? — pushary](https://pushary.com/blog/what-permissions-should-an-ai-agent-have)

### 腾讯 WorkBuddy（专家与专家团参照）

- [WorkBuddy 官网](https://www.workbuddy.cn/) · [Tencent Cloud 产品页](https://cloud.tencent.com/product/workbuddy) · [企业版](https://cloud.tencent.com/product/workbuddy-enterprise)
- [专家中心 — WorkBuddy 官方文档](https://www.workbuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Expert-Center)
- [WorkBuddy 能力体系：专家智能体、Skill、MCP — 知乎](https://zhuanlan.zhihu.com/p/2047676983740470493)
- [WorkBuddy 专家团新功能：一句话定制专属 AI 团队 — 知乎](https://zhuanlan.zhihu.com/p/2045911727477469619)
- [WorkBuddy 硬核实战：自定义 Skill 与 MCP 开发全指南 — 知乎](https://zhuanlan.zhihu.com/p/2077749253108192294)
- [WorkBuddy 进阶教程：Skills、MCP、记忆与多任务实战 — 七牛云](https://news.qiniu.com/archives/1786500985404)
- [What Is WorkBuddy? A Complete Guide — Tencent Cloud](https://www.tencentcloud.com/techpedia/145645?lang=en)
- [Tencent Lays Out Its Full AI Agent Strategy With WorkBuddy Enterprise — Pandaily](https://pandaily.com/tencent-workbuddy-enterprise-agent-suite-strategy-jun2026)
- [从超级个体到超级团队，腾讯云发布 WorkBuddy 企业版 — 量子位](https://www.qbitai.com/2026/06/430758.html) · [网易转载](https://www.163.com/dy/article/KUM46N730511DSSR.html)

### 专家层机制（本仓库内部依据）

- [`opensource/workbuddy/我用阿里 AgentScope 复刻了一个 WorkBuddy：专家团是怎么跑起来的.md`](./opensource/workbuddy/我用阿里%20AgentScope%20复刻了一个%20WorkBuddy：专家团是怎么跑起来的.md) —— 叶小钗，2026-08-12（[原文](https://mp.weixin.qq.com/s/TcNThG1XRsaHNt9kGxXN4A)）。专家五要素、专家/专家团目录包结构与 `plugin.json` 字段、主理人行为契约、依赖驱动任务图、版本指纹、事件投射、异步回合语义。§3.8 的依据
- `node_modules/@deepseek-ai/dsh-subagent-in-process-driver/README.md` —— 子智能体运行生命周期、独立 persona 与工具限制、注册作用域不继承、sandbox / 审批钉选传播、深度限制
- [DSH_PLUGIN_GUIDE.md](./DSH_PLUGIN_GUIDE.md) —— 通过 patch `insert` 补装插件包的机制
- `packages/dsh-bridge/src/DshApiServer.ts:3039`、`3125`、`3139` —— 当前 team / 多智能体对 Direct DSH 后端关闭的代码位置

> 检索时间：2026-09-11。第 3 章中 Microsoft Learn、Gemini API 官方文档与 WorkBuddy 官方文档三处的直接抓取被网络策略拦截，相关表述基于检索结果摘要，实施前建议复核原文——**3.7 中关于 WorkBuddy 的字段定义、成本倍数与会话约束三项尤其建议以官方文档为准**。第 1.3 节的 DSH 子智能体语义取自本地 package README，实施前建议以实际 `acp` profile 的插件组成为准复核。
