# 办公模式与编码模式差异分析

## 1. 结论

当前办公模式和编码模式不是两套完全不同的模型或工具系统，而是建立在同一套 DeepSeek Harness 能力之上的两种会话身份。

两者通过以下机制形成差异：

1. 使用不同的稳定助手 ID。
2. 向 DeepSeek Harness 注入不同的系统 Persona。
3. 使用两个独立、按需启动的 DSH ACP 运行时。
4. 使用不同的 DSH 状态目录保存会话和运行时数据。
5. 将模式固化到会话记录中，恢复会话时继续进入原模式。

因此，当前模式的准确定位是“行为倾向与运行时状态隔离”，而不是“办公能力与编码能力的硬性权限隔离”。

## 2. 核心差异

| 维度         | 办公模式                              | 编码模式                                      |
| ------------ | ------------------------------------- | --------------------------------------------- |
| 稳定 ID      | `dsh:office`                          | `dsh:coding`                                  |
| 主要目标     | 文档、表格、演示、PDF、调研和资料整理 | 仓库分析、代码修改、测试、终端和工程验证      |
| 输出偏好     | 面向实际业务交付，减少无关工程细节    | 面向可验证的代码结果，说明测试和剩余风险      |
| 工作方法     | 优先确认目标格式、交付形式和覆盖范围  | 优先阅读仓库规范、控制修改范围并运行检查      |
| DSH 状态目录 | `dshHome/modes/office`                | 原始 `dshHome`                                |
| 旧会话兼容   | 不承接旧版 DeepSeek Harness 会话      | 旧 ID `dsh:deepseek-harness` 自动映射到此模式 |
| 界面图标     | 公文包图标                            | 代码图标                                      |

## 3. Persona 行为差异

### 3.1 办公模式

办公模式的 Persona 要求模型优先处理：

- 商务写作和内容润色；
- Word、Excel、PPT、PDF 等办公材料；
- 网页调研、资料汇总和数据整理；
- 面向业务人员可直接使用的最终交付物；
- 在目标、格式或覆盖范围不明确时先进行确认。

办公模式仍然可以使用脚本和终端。当脚本能够提升处理效率时，模型可以编写代码，但应把代码视为完成办公交付的手段，而不是最终目标。

### 3.2 编码模式

编码模式的 Persona 要求模型优先处理：

- 阅读并理解仓库结构和开发规范；
- 定位代码路径、依赖关系和运行时边界；
- 实施范围明确、可验证的代码修改；
- 运行与风险相匹配的测试、类型检查和静态检查；
- 清晰报告修改内容、验证状态和剩余风险。

编码模式仍然可以撰写文档、整理表格或生成演示内容，只是默认思考方式更偏向软件工程工作流。

## 4. 运行时隔离

系统按模式维护两个延迟启动的 DSH 运行时：

```text
办公会话 -> Office DSH 进程 -> Office Persona
编码会话 -> Coding DSH 进程 -> Coding Persona
```

这种设计具有以下效果：

- 两个 Persona 不会在同一个 DSH 进程中互相覆盖；
- 办公模式和编码模式的会话缓存、配置与恢复状态分开存放；
- 一个模式启动失败时，不会直接销毁另一个模式的运行时；
- 同一模式下的多个会话共享该模式的 DSH 进程，但使用不同 ACP Session；
- 两个模式都被使用后，应用需要维持两个 DSH 进程，会增加一定内存占用。

状态目录隔离不等于工作目录隔离。两个模式仍然可以指向同一个项目目录，因此并发修改同一文件时仍可能产生冲突。

## 5. 会话生命周期

创建会话时，系统根据助手 ID 写入标准化模式字段：

```text
conversation.extra.work_mode = office | coding
```

后续创建 ACP Session、恢复历史 Session、发送提示、取消任务和更新模型配置时，都会路由到该模式对应的运行时。

模式在单个会话中保持不变。通用会话更新接口不能把编码会话改成办公会话，反之亦然。需要切换工作模式时，应从主界面选择另一模式并创建新会话，避免历史消息中的系统目标发生冲突。

兼容规则如下：

- 缺少 `work_mode` 的旧会话默认按编码模式解释；
- 旧助手 ID `dsh:deepseek-harness` 映射到 `dsh:coding`；
- 编码模式继续使用原始 `dshHome`，以便恢复旧版 DSH Session；
- 不批量改写旧数据，而是在读取和恢复时完成兼容映射。

## 6. 两种模式当前相同的能力

办公模式和编码模式当前共享以下配置：

- 相同的 DeepSeek 模型及模型列表；
- 相同的 `DEEPSEEK_API_KEY` 和模型供应商路由；
- 相同的工作区和文件访问范围；
- 相同的 MCP 服务；
- 相同的终端、脚本和文件处理能力；
- 相同的 `workspace-write` 沙箱策略；
- 相同的 `approval: ask` 审批策略；
- 相同的流式输出、取消、模型切换和会话恢复机制；
- 相同的 Team Assistant 限制；
- 相同的图片和音频提示能力声明。

这意味着办公模式不会禁止编码，编码模式也不会禁止办公操作。Persona 负责影响模型的默认取舍，但不是安全边界，也不是工具访问控制规则。

## 7. 用户可感知的实际效果

对于目标模糊的任务，两种模式的差异最明显。

例如输入“整理这批材料”：

- 办公模式更可能先识别交付对象和格式，输出摘要、报告、表格或演示材料；
- 编码模式更可能检查目录结构、编写处理脚本，并报告实现方法和验证结果。

对于目标非常明确的任务，Persona 的影响会减弱。

例如明确要求“修改 TypeScript 文件并运行测试”时，办公模式仍可执行编码任务；明确要求“生成一份会议纪要”时，编码模式也能完成文档工作。因此，模式决定的是默认优先级，而不是绝对能力范围。

## 8. 当前实现的边界

当前第一版尚未提供以下硬差异：

- 没有办公模式专属的 MCP 或 Skills；
- 没有编码模式专属的终端和代码分析工具；
- 没有按模式区分模型、温度、上下文窗口或推理参数；
- 没有按模式设置不同的沙箱和审批权限；
- 没有办公模式专属的 Word、Excel、PPT 模板工作流；
- 没有按模式限制可访问的文件类型；
- 没有隔离两个模式所操作的实际工作区文件。

所以，“办公模式更擅长办公”目前主要来自系统提示词引导，而不是额外的 Office 自动化能力。

## 9. 后续增强方向

如果需要让两种模式形成更强、更稳定的产品差异，可以继续增加：

1. 为办公模式配置文档解析、Office 自动化、数据清洗和模板生成 Skills。
2. 为编码模式配置代码搜索、语言服务器、测试分析和版本控制 Skills。
3. 根据模式提供不同的 MCP 白名单和默认启用项。
4. 为办公模式增加交付格式、模板和输出目录设置。
5. 为编码模式增加测试策略、变更审阅和仓库规范检测。
6. 在界面中展示简短的模式能力说明，而不只显示名称和图标。
7. 为两个模式建立相同输入下的行为评测，验证 Persona 是否产生稳定差异。

## 10. 总结

当前实现已经解决了“两个按钮只是改文字”的问题：模式会进入会话数据、影响系统 Persona，并路由到独立的 DSH 运行时。

但它仍属于行为层的分工，而非工具层的强隔离。办公模式和编码模式具备相同基础能力，差异主要体现在默认目标、处理流程、表达方式和运行时状态。若产品目标是提供真正专业化的办公与开发环境，下一阶段应把差异继续下沉到 Skills、MCP、模板、权限和验收机制。
